package xasync

import scala.util.chaining._

object Optionally extends hutil.App {

  // val res = optionally {
  //   val x = Option(2)
  //   val y = Option(3)
  //   x.value + y.value
  // }

  "optionally -- not yet available".pipe(println)
}
